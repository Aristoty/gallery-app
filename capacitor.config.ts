import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mobile.gallery',
  appName: 'gallery-app',
  webDir: 'dist/angular-app/browser'
};

export default config;
